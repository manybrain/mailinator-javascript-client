import { version } from '../package.json';

export const AUTHORIZATION: string = "Authorization";
export const USERAGENT: string = "User-Agent";
export const COMMON_USER_AGENT = `Mailinator SDK - JS V${version}`;